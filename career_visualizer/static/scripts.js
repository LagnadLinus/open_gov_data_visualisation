

document.addEventListener('DOMContentLoaded', () => {
    document.getElementById('yearRange').addEventListener('input', () => {
        // Capture year range
    });
    
    document.getElementById('incomeRange').addEventListener('input', () => {
        // Capture income range
    });
    
    // Similar for checkboxes
    
});


